interface:
  display_name: "ABC Calibration"
  short_description: "Likelihood-free calibration with exact user bounds"
  default_prompt: "Use $abc-calibration to inspect a simulator or model, require explicit user-provided bounds for every parameter, and build or run a two-phase ABC-Rejection calibration workflow that enforces those exact bounds during sampling."
